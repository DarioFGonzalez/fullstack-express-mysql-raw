# Changelog

## 2026-03-23

### Schema changes
- Dropped `users` table, created `clients` with:
  - UUID as primary key
  - business_name, tax_id, email (UNIQUE)
  - password, verification_token, verified_at
  - is_active defaults to false

### Next steps
- [ ] POST /clients/register with bcrypt + token generation
- [ ] GET /clients/verify?token=xxx
- [ ] POST /clients/login
- [ ] CRUD for products
- [ ] Invoices with invoice_items

### Notes
- Using raw MySQL queries (no ORM)
- Connection pool configured
- UUID generated by MySQL DEFAULT (UUID())